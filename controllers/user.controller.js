//here we write logic of app

export const homeController = (req_, res) => {
    res.send("App is running from home controller ");
}

export const userController = (req_, res) => {
    res.send("App is running from home controller user routes ");
}
